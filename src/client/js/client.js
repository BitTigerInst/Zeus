autoScrollingIsActive = false; // need var?
/* reactive var here */
thereAreUnreadMessages = new ReactiveVar(false); // must be global

counts = new ReactiveVar(0);

// /* new */
// var ITEMS_INCREMENT = 20;
// Session.setDefault('itemsLimit', ITEMS_INCREMENT);
